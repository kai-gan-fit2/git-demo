逻辑卷扩容

查看挂载情况

lvs

lsblk

df -h

初始化逻辑盘

pvcreate /dev/sdb

查看正确的卷组名

vgs

把 /dev/sdb 加入卷组 ubuntu-vg

vgextend ubuntu-vg /dev/sdb

执行扩展

lvextend -l +100%FREE /dev/ubuntu-vg/ubuntu-lv

查看文件系统类型

df -T /

针对 **ext4** 文件系统，扩展逻辑卷后需使用 `resize2fs` 同步容量

resize2fs /dev/ubuntu-vg/ubuntu-lv